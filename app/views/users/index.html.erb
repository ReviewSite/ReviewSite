<h1>Manage Users</h1>

<%= link_to 'New User', new_user_path, :class => 'btn btn-info' %>

<table class="datatable">
  <thead>
  <th>Name</th>
  <th>Email</th>
  <th>JC</th>
  <th>Admin</th>
  <th></th>
  <th></th>
  <th></th>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <tr id="user_<%= user.id %>">
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td class="jc" ><%= user.jc? %></td>
        <td><%= user.admin %></td>
        <td>
          <% if can? :read, user %>
              <%= link_to 'View Profile', user %>
          <% end %>
        </td>
        <td>
          <% if can? :edit, user %>
              <%= link_to 'Edit', edit_user_path(user) %>
          <% end %>
        </td>
        <td class="delete">
          <% if can? :destroy, user %>
              <%= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %>
          <% end %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>

<br class="spacer" />


<script>
    $('.datatable').dataTable();
</script>
